/**
 * Token url request.
 */
 export interface GenerateUrlRequestModel {
    /**
     * User id.
     */
    id: string;

    /**
     * Used to generata url to view.
     */
    clientUrl: string;
}